{
  "name": "@keel/flutter-sdk",
  "version": "0.3.0",
  "private": true,
  "description": "Keel Flutter SDK — client library for Flutter apps (BaaS)",
  "scripts": {
    "build": "echo 'Flutter SDK: run flutter pub get && flutter analyze'",
    "test": "echo 'Flutter SDK: run flutter test'",
    "clean": "rm -rf build .dart_tool"
  }
}
